import { Box } from '@mui/material';
import React from 'react';

const About = () => {
    return (
        <Box sx={{textAlign: 'center', fontSize: '40px', textTransform: 'capitalize', fontWeight: 'bolder', lineHeight: '90vh',color: 'aqua'}}>
            about Page
        </Box>
    );
};

export default About;